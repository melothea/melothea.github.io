{
    "name": "song_artists_sources",
    "columns": [
        "id",
        "parent_id",
        "source_id"
    ],
    "schema": "CREATE TABLE \"song_artists_sources\" (\n  id INTEGER PRIMARY KEY,\n  parent_id INTEGER NOT NULL REFERENCES song_artists(id),\n  source_id INTEGER NOT NULL REFERENCES sources(id)\n)"
}