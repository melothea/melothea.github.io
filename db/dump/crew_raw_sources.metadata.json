{
    "name": "crew_raw_sources",
    "columns": [
        "id",
        "parent_id",
        "source_id"
    ],
    "schema": "CREATE TABLE \"crew_raw_sources\" (\n  id INTEGER PRIMARY KEY,\n  parent_id INTEGER NOT NULL REFERENCES crew_raw(id),\n  source_id INTEGER NOT NULL REFERENCES sources(id)\n)"
}