{
    "name": "memberships_sources",
    "columns": [
        "id",
        "parent_id",
        "source_id"
    ],
    "schema": "CREATE TABLE \"memberships_sources\" (\n  id INTEGER PRIMARY KEY,\n  parent_id INTEGER NOT NULL REFERENCES memberships(id),\n  source_id INTEGER NOT NULL REFERENCES sources(id)\n)"
}