# Python依存（フルstation間の再現性のためのロック。ROADMAPフェーズマイナス1）
# 直接依存：sqlite-diffable（SQLite正本→JSONLダンプの生成／往復ロード）
# 生成方法：python3 -m venv .venv && .venv/bin/pip install sqlite-diffable && .venv/bin/pip freeze
# Python 3.12 で固定（.venv はリポジトリに含めない＝各マシンで一度生成）。
click==8.4.2
click-default-group==1.2.4
pluggy==1.6.0
python-dateutil==2.9.0.post0
six==1.17.0
sqlite-diffable==0.6
sqlite-fts4==1.0.3
sqlite-utils==3.39
tabulate==0.10.0
